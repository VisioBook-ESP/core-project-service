name: core-project-service
replicaCount: 1
image:
  repository: ghcr.io/visiobook-esp/core-project-service-dev
  tag: "sha-3359a89"
  pullPolicy: IfNotPresent

strategy:
  type: RollingUpdate
  rollingUpdate:
    maxUnavailable: 0
    maxSurge: 1

revisionHistoryLimit: 3
progressDeadlineSeconds: 60
minReadySeconds: 10
terminationGracePeriodSeconds: 10

syncWave: "0"

existingSecret: ""
existingSecretPgcrud: ""

service:
  type: ClusterIP
  port: 8086

istio:
  enabled: true
  injection: true

  virtualService:
    enabled: true
    hosts:
      - "core-project-service"
      - "core-project-service.visiobook.dev"
    gateways:
      - mesh
  destinationRule:
    enabled: true
    trafficPolicy:
      connectionPool:
        tcp:
          maxConnections: 100
        http:
          http1MaxPendingRequests: 100
          http2MaxRequests: 100
          maxRequestsPerConnection: 2
      loadBalancer:
        simple: ROUND_ROBIN
      outlierDetection:
        consecutiveErrors: 5
        interval: 30s
        baseEjectionTime: 30s

  mtls:
    enabled: true
    mode: ISTIO_MUTUAL

nodeSelector: {}
tolerations: []
affinity: {}

podLabels:
  app: core-project-service
  version: "latest"
  app.kubernetes.io/name: core-project-service
  app.kubernetes.io/version: "latest"
  app.kubernetes.io/component: backend

selectorLabels:
  app: core-project-service
  version: latest

resources:
  requests:
    cpu: 250m
    memory: 256Mi
  limits:
    cpu: 1000m
    memory: 512Mi

env:
  APP_NAME: core-project-service
  APP_VERSION: 0.1.0
  APP_PORT: "8086"
  NODE_ENV: production
  LOG_LEVEL: info
  SWAGGER_ENABLED: "false"
  REDIS_HOST: redis-core-project.visiobook-namespace.svc.cluster.local
  REDIS_PORT: "6379"
  NATS_URL: nats://nats.visiobook-namespace.svc.cluster.local:4222
  USER_SERVICE_URL: http://core-user-service.visiobook-namespace.svc.cluster.local:8081

probes:
  readiness:
    path: /health/ready
    initialDelaySeconds: 10
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 3
  liveness:
    path: /health/live
    initialDelaySeconds: 15
    periodSeconds: 15
    timeoutSeconds: 5
    failureThreshold: 3

hpa:
  enabled: false
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilization: 70

serviceMonitor:
  enabled: false
  interval: 30s
