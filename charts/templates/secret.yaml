apiVersion: v1
kind: Secret
metadata:
  name: {{ .Chart.Name }}-secret
type: Opaque
data: {}
  # Populated by external secret operator or manually:
  # DATABASE_URL: (base64 encoded)
  # REDIS_PASSWORD: (base64 encoded)
  # NATS_USER: (base64 encoded)
  # NATS_PASSWORD: (base64 encoded)
