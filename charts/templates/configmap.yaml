apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "core-project-service.fullname" . }}-config
  labels:
    {{- include "core-project-service.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
