replicaCount: 3

resources:
  requests:
    cpu: 500m
    memory: 512Mi
  limits:
    cpu: 2000m
    memory: 1024Mi

env:
  NODE_ENV: production
  LOG_LEVEL: info
  SWAGGER_ENABLED: 'false'

hpa:
  minReplicas: 3
  maxReplicas: 10
