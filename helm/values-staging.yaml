replicaCount: 2

resources:
  requests:
    cpu: 250m
    memory: 256Mi
  limits:
    cpu: 1000m
    memory: 512Mi

env:
  NODE_ENV: staging
  LOG_LEVEL: info
  SWAGGER_ENABLED: 'true'

hpa:
  minReplicas: 2
  maxReplicas: 5
